{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Default permissions for the editor window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
